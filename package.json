{
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^5.0.0",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0"
  },
  "scripts": {
    "start": "hugo serve",
    "build": "hugo --gc --minify --environment=production"
  }
}
